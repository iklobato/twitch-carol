# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/

# Local data (sim audio input, local audio store)
data/

# Node
node_modules/
apps/web/dist/

# Local data/artifacts
chat-log.txt
chat-log.csv
emails.txt
emails_extracted.txt
.playwright-mcp/

# Local agent config
.claude/

# Local screenshots
# Screenshots dropped in the repo root. The list used to be per-name and
# still let access-error.png and grafana-access-login.png through on a
# `git add -A`. No png is versioned anywhere in this repo, so ignore them all.
/*.png

# Secrets
.env
